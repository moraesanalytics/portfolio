# Assets

Static assets imported by components (images, screenshots, illustrations).

- `dashboard/` — screenshots of the NorthWind Distribution Analytics dashboard (to replace the CSS-built preview when available).
